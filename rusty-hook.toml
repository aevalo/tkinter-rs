[hooks]
# the hook-as-array feature support was added in the unusable, unreleased v0.12.0
#pre-commit = ["cargo check", "cargo fmt -- --check", "cargo clippy --workspace --all-targets --all-features --frozen"]
pre-commit = "cargo check"
pre-push = "cargo test"

[logging]
verbose = true
